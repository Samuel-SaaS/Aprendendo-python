 # if → basicamente um se
    #faz algo
 # else → senão
   #faz algo
 # elif → else/if, vários casos
    #faz algo
 # match (variável) → usar em vários casos, tipo elif, mas pq só não usar elif?
    # case (resposta):
           #faz algo
    # case _: → else
           #faz algo
