# len() → conta quantos itens tem no que tá no parentes, se for str, contará quantas letras
# X.find(' ') → contará quantos itens tem até chegar no item do parentes, começando do 0
# X.rlen(' ') → Contará quantos itens tem até chegar no último item do parenteses, começando do 0
# X.Capitalize() → capitalizará o texto
# X.Upper() → deixará todos as letras maiúsculos
# ..., mesmo esquema com X.isalpha, etc

nome = input('Digite seu nome: ')

# a = len(nome)
# a = nome.find(' ')
# a = nome.rfind(' ')
# a = nome.subs()
a = nome.alpha('')

print(a)